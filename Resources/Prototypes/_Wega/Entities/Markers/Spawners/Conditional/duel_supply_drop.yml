# Спавнер дуэльного ящика снаряжения.
# Размещается на карте в центре арены.
#
# Каждые 45 секунд спавнит МАЯК DuelSupplyDropBeacon в случайной точке арены
# (randomSpawnRadius — в радиусе от центра, спавнер сам ищет валидный тайл, минуя
# стены и космос). Маяк ярко горит и мигает + проигрывает сигнал на весь сервер,
# а через 5 секунд на его месте падает сам ящик CrateDuelSupplyDrop. Так все
# дуэлянты успевают увидеть, КУДА именно прилетит снаряжение, и сбежаться к точке.
#
# randomSpawnRadius задаёт разброс точек падения — подгони под размер своей арены
# в редакторе (трекер/спавнер ставь в центр).
#
# Спавнер ВЫКЛЮЧЕН по умолчанию (enabled: false).
# Кнопка DuelSupplyDropButton автоматически связывается через AutoLink (канал DuelDrop).
# Нажатие кнопки переключает спавнер:
#   ВЫКЛ → ВКЛ  (первый маяк через 45 сек после нажатия)
#   ВКЛ → ВЫКЛ  (останавливает аирдропы)

- type: entity
  name: Duel Supply Drop Spawner
  id: DuelSupplyDropSpawner
  parent: MarkerBase
  components:
  - type: Sprite
    layers:
      - state: red
      - state: timed
  - type: TimedSpawner
    prototypes:
      - DuelSupplyDropBeacon
    chance: 1.0
    intervalSeconds: 45
    minimumEntitiesSpawned: 1
    maximumEntitiesSpawned: 1
    randomSpawnRadius: 10.0  # разброс точек падения; подгони под размер арены
    enabled: false
  - type: SpawnerSignalControl
  - type: DeviceNetwork
    deviceNetId: Wireless
    receiveFrequencyId: BasicDevice
  - type: WirelessNetworkConnection
    range: 200
  - type: DeviceLinkSink
    ports:
      - Toggle
  - type: AutoLinkReceiver
    channel: DuelDrop

# Кнопка для управления спавнером дуэльного ящика.
# Размещается на стене арены, AutoLink автоматически связывает с DuelSupplyDropSpawner
# через канал DuelDrop — ручная настройка в редакторе не нужна.
# Нажатие кнопки переключает спавнер (ВКЛ/ВЫКЛ).
- type: entity
  name: duel supply drop button
  id: DuelSupplyDropButton
  parent: SignalButton
  suffix: Duel Arena
  description: Запускает или останавливает периодический сброс ящиков снаряжения в центр арены.
  components:
  - type: DeviceLinkSource
    ports:
      - Pressed
    range: 200  # арена может быть большой — нужен запас
  - type: AutoLinkTransmitter
    channel: DuelDrop
